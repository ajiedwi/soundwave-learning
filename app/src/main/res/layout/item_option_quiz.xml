<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="60dp"
    android:layout_marginEnd="20dp"
    android:layout_marginStart="20dp"
    android:layout_marginTop="5dp"
    android:layout_marginBottom="5dp"
    android:paddingTop="10dp"
    android:paddingBottom="10dp"
    android:id="@+id/cl_option_quiz"
    android:background="@drawable/bg_quiz_unselected">

    <TextView
        android:id="@+id/tv_item_quiz_option"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/item_option_quiz_true"
        android:layout_marginStart="20dp"
        android:textColor="@color/chocolate"
        android:fontFamily="@font/raleway_reguler"
        android:textSize="12sp"
        android:layout_marginEnd="10dp"
        android:text="Materi\ncoba"/>

    <ImageView
        android:visibility="invisible"
        android:id="@+id/item_option_quiz_true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="20dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:src="@drawable/ic_check_green_light"/>

</androidx.constraintlayout.widget.ConstraintLayout>